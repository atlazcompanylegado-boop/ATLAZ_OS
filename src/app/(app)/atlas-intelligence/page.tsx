import { Sparkles } from "lucide-react";
import { ModulePlaceholder } from "@/components/layout/module-placeholder";

export default function AtlasIntelligencePage() {
  return (
    <ModulePlaceholder
      title="Atlas Intelligence"
      phase="Fase 5"
      icon={Sparkles}
      description="IA interna: sugestão de tema, hook, roteiro, legenda e CTA a partir do histórico real de conteúdo — e, depois, recomendações para CRM, projetos, suporte e financeiro."
    />
  );
}
