import { Instagram } from "lucide-react";
import { ModulePlaceholder } from "@/components/layout/module-placeholder";

export default function AtlasSocialPage() {
  return (
    <ModulePlaceholder
      title="Atlas Social"
      phase="Fase 3"
      icon={Instagram}
      description="Central de conteúdo: ideia → roteiro → produção → edição → revisão → programado → publicado, com analytics do Instagram ligado ao CRM."
    />
  );
}
